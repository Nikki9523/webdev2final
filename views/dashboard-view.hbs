{{> menu active="dashboard"}}

<section class="section">
  {{> add-station}}
</section>

<section class="section">
  {{> list-stations}}
</section>

<section class="section">
  {{>station-map}}
</section>